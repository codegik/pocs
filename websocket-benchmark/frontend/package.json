{
  "name": "websocket-benchmark-frontend",
  "version": "1.0.0",
  "description": "Frontend for WebSocket vs HTTP performance comparison",
  "scripts": {
    "start": "serve -s ."
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}

