{
  "name": "websocket-benchmark-backend",
  "version": "1.0.0",
  "description": "Backend server for WebSocket vs HTTP performance comparison",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.13.0",
    "cors": "^2.8.5",
    "prom-client": "^14.2.0"
  }
}

